# Research Engineer Take-Home Assignment — Dependencies
# Install with:  pip install -r requirements.txt
#
# Tested on Python 3.10 and 3.11.  No GPU required.

# ── Core ─────────────────────────────────────────────────────────────────────
torch==2.3.1
numpy==1.26.4
matplotlib==3.9.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0

# ── Task 3B: API server ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.7.0

# ── Task 2B: Foundation model evaluation ─────────────────────────────────────
# lerobot provides SmolVLA, ACT, and the pusht dataset in one package.
# Install this first — it pulls in huggingface_hub, datasets, and gym_pusht.
lerobot>=0.3.0

# ── Task 3A: LLM API — uncomment your chosen provider ────────────────────────
# anthropic==0.28.0
# openai==1.30.0
google-generativeai>=0.3.0
python-dotenv>=1.0.0
# (For local Ollama, no extra package is required — uses urllib from stdlib)
